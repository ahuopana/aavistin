# demo-widget-risks

A small, fictional risk catalog whose triggers reference
`demo-widget-safety`'s questions (`has_wireless`, `rated_power_watts`),
the same way a real catalog's triggers would reference a real
regulation package's questions. Entirely invented for tests, like
`demo-widget-safety` — see that package's own README.
