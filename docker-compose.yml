services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aavistin
      POSTGRES_PASSWORD: aavistin
      POSTGRES_DB: aavistin
    ports:
      - "5432:5432"
    volumes:
      - aavistin_pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aavistin"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    image: aavistin:dev
    build: .
    command: >
      sh -c "uv run manage.py migrate &&
             uv run manage.py seed_dev_users &&
             uv run manage.py runserver 0.0.0.0:8000"
    volumes:
      - .:/app
      - aavistin_uv_cache:/root/.cache/uv
    ports:
      - "8000:8000"
    environment:
      DEBUG: "True"
      DJANGO_SECRET_KEY: dev-secret-key-not-for-production
      DJANGO_ALLOWED_HOSTS: localhost,127.0.0.1
      DATABASE_URL: postgres://aavistin:aavistin@db:5432/aavistin
    depends_on:
      db:
        condition: service_healthy

  # Background jobs (docs/adr/0007-background-job-backend.md): worker
  # processes tasks enqueued to the Postgres-backed queue; scheduler
  # enqueues the periodic ones (e.g. staleness recompute) on an
  # interval. Neither needs its own service beyond Postgres.
  worker:
    image: aavistin:dev
    build: .
    command: sh -c "uv run manage.py migrate && uv run manage.py db_worker"
    volumes:
      - .:/app
      - aavistin_uv_cache:/root/.cache/uv
    environment:
      DEBUG: "True"
      DJANGO_SECRET_KEY: dev-secret-key-not-for-production
      DJANGO_ALLOWED_HOSTS: localhost,127.0.0.1
      DATABASE_URL: postgres://aavistin:aavistin@db:5432/aavistin
    depends_on:
      db:
        condition: service_healthy

  scheduler:
    image: aavistin:dev
    build: .
    command: uv run manage.py scheduler
    volumes:
      - .:/app
      - aavistin_uv_cache:/root/.cache/uv
    environment:
      DEBUG: "True"
      DJANGO_SECRET_KEY: dev-secret-key-not-for-production
      DJANGO_ALLOWED_HOSTS: localhost,127.0.0.1
      DATABASE_URL: postgres://aavistin:aavistin@db:5432/aavistin
    depends_on:
      db:
        condition: service_healthy

volumes:
  aavistin_pgdata:
  # uv's package cache, shared across services that bind-mount the
  # project root over /app: that mount shadows the image's baked-in
  # /app/.venv, but `uv run` (see the compose commands above) re-syncs
  # a correct venv from uv.lock on every container start regardless.
  # This volume just makes repeat syncs fast — it never needs manual
  # invalidation the way a venv-holding volume would after a dependency
  # change, since it's an additive download cache, not the venv itself.
  # All three services share one `image:` tag too, so they're always
  # built from identical content regardless of which container starts
  # first.
  aavistin_uv_cache:
